version: 2.1

workflows:
  ci:
    jobs:
      - build-and-test
   
jobs:
  build-and-test:
    docker:
      # using a base image 
      - image: cimg/base:stable
    steps:
      - checkout
      - run: 
          name: Run Tests
          command: 
            # use sudo to install on docker with non-root user
            sudo apt-get install npm
            # npm run test 
      